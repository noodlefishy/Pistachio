product:
  type: kmp/lib
  platforms: [jvm, wasmJs]

settings:
  jvm:
    release: 17
  kotlin:
    serialization: json

dependencies:
  - org.jetbrains.kotlinx:kotlinx-coroutines-core:1.8.1
  - $kotlin.serialization.json
  - $kotlin.test
test-dependencies:
  - $kotlin.test