main:
    movi r6, stack
    call heap_init

	movi r1 1
	call alloc

	mov r5 r1

    mov r1 r5
    movi r2, #30
    call insert_at_front

    mov r1 r5
    movi r2, #20
    call insert_at_front

    mov r1 r5
    movi r2, #10
    call insert_at_front

    mov r1 r5
    lw   r1, [r1 + #0]
    call print_list

    mov r1 r5
    lw   r1, [r1 + #0]
    call free_list

    mov r1 r5
    sw   r0, [r1 + #0]

    halt








stack:    .space 100